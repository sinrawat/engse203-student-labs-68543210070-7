# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** ศิลวัต อาซอง **รหัส:** 68543210070-7 **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 15.09 | B1 | ขอแนวทางตรวจสอบปัญหาใน React ที่เกี่ยวกับการแสดงรายการ | แนะนำให้ตรวจสอบการใช้ key ในการ render รายการด้วย map เพื่อให้ React ระบุแต่ละรายการได้ถูกต้อง | ตรวจสอบส่วนที่ใช้ map ใน SummaryPanel.jsx และเพิ่ม key ให้กับรายการ จากนั้นตรวจสอบ Warning ใน Console |
| 15.09 | B1 | ขอแนวทางตรวจสอบการกรองข้อมูลตามสถานะ | แนะนำให้ตรวจสอบเงื่อนไข status ที่ใช้ในการ filter ว่าตรงกับสถานะที่ต้องการหรือไม่ | แก้เงื่อนไขการนับ completed และตรวจสอบผลลัพธ์ของการกรองแต่ละสถานะ |
| 15.09 | B1 | ขอแนวทางแก้ปัญหา filter ที่เลือกแล้วแสดงข้อมูลไม่ตรง | แนะนำให้ใช้ค่าของ statusFilter เป็นเงื่อนไขในการกรองแทนการกำหนดสถานะไว้ตายตัว | ปรับเงื่อนไขให้ใช้ statusFilter และทดสอบการเปลี่ยนตัวกรองแต่ละสถานะ |
| 15.09 | B1 | ขอแนวทางตรวจสอบข้อมูลที่ไม่อัปเดตเมื่อเปลี่ยน URL | แนะนำให้ตรวจสอบ dependency ของ useEffect และเพิ่มค่าที่ effect ใช้งานลงใน dependency array | แก้ dependency ของ useEffect ให้ทำงานใหม่เมื่อ requestId เปลี่ยน และทดสอบเปลี่ยน URL จาก REQ-101 เป็น REQ-102 |
| 15.09 | B1 | ขอแนวทางแก้ข้อมูล Summary ที่ไม่อัปเดตหลังข้อมูลเปลี่ยน | แนะนำให้ตรวจสอบ dependency ของ useMemo เพื่อให้คำนวณใหม่เมื่อ requests เปลี่ยน | แก้ dependency ของ useMemo จากค่าคงที่เป็น requests และทดสอบหลังลบรายการ |
|  15.09| B1 | ขอแนวทางแก้ปัญหาการนำ Promise ไปเก็บใน state | แนะนำให้ใช้ async/await เพื่อรอผลลัพธ์จากฟังก์ชันที่คืนค่า Promise ก่อนนำไป setState | ปรับ handleDelete ให้ await deleteRequest ก่อน setRequests และทดสอบการลบรายการว่าสรุปผลและหน้าเว็บยังทำงานปกติ |
| 15.27 | B2 | ขอแนวทางการเพิ่มช่องค้นหาใน Dashboard | แนะนำให้เก็บค่าการค้นหาด้วย useState และนำค่าที่ได้ไปใช้กรองรายการ | ศึกษาแนวทางแล้วนำมาปรับใช้กับ DashboardPage.jsx และทดสอบการกรอกข้อความ |
| 16.28 | B2 | ขอแนวทางการกรองข้อมูลจากหลาย field | แนะนำให้ใช้ filter และตรวจ requestType หรือ location โดยใช้ toLowerCase และ includes | ปรับเงื่อนไขการกรองให้ค้นหาจากประเภทหรือสถานที่ และทดสอบด้วยคำว่า "ซ่อม" |
| 16.31 | B2 | ตรวจสอบกรณีค้นหาแล้วไม่พบข้อมูลควรจัดการอย่างไร | แนะนำให้ตรวจจำนวนรายการหลังกรอง และแสดงข้อความเมื่อไม่มีข้อมูล โดย Summary ยังคำนวณจากข้อมูลทั้งหมด | เพิ่มข้อความแจ้งเมื่อไม่พบรายการ และตรวจสอบว่า Summary ยังคงแสดงจำนวนข้อมูลทั้งหมด |

*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ศิลวัต อาซอง
